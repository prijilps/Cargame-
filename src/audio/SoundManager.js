// SoundManager.js
// Handles engine, tire, collision, UI, and ambient sounds

export class SoundManager {
  playEngine(car) {}
  playTireSqueal(car) {}
  playCollision() {}
  playUI() {}
  playAmbient() {}
}
